# from typing import Any

# def register_movie(actors: list[str], directors: list[str], movie: dict[str, Any]) -> None:
# check if movie is already in db


# check if actor(s) already in db

# check if directors already in db
